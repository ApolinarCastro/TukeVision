# E1 focused evidence

The existing evidence index was revalidated against the bounded persistent
store: every persisted frame and metadata file resolved and every SHA-256
matched. The original validator's failure is retained as historical evidence;
no destructive rewrite was performed.
