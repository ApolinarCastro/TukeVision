# Synthetic clip demo

Synthetic frames → `EvidenceClipAdapter` → MP4 → SHA-256 verification passed.
Empty input returns `availability=UNAVAILABLE` without raising or creating a
false reference. Focused QW-04 tests: 3/3 PASS.
